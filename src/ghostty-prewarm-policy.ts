// Three-run Chromium measurements across mobile solo, desktop solo, and a
// two-cell desktop grid retained first-open latency while saving one idle WASM
// backing store versus a pool of two. See .plans/004-terminal-prewarm-measurements.md.
export const DEFAULT_GHOSTTY_PREWARM_POOL_SIZE = 1;
