# issues #231 and #232

status: implementation in progress
branch: `fix/issues-231-232` (from `main` at `38c6b64`)

## scope

1. #231 — add a ring-eviction replay-truncation regression test and correct the stale broker comment.
2. #232 — its four findings are independent: unused exports, the terminal-controller decomposition, browser test mirrors, and incremental browser TypeScript strictness.

## proposed sequence

1. inspect #231's output/replay contracts and add its regression test first.
2. triage #232 into independently verifiable changes, avoiding a mixed behavior/refactor change.
3. run the relevant Rust and Bun test/typecheck commands after each change, then the full applicable suites.

## progress

- [x] #231 regression test and stale ring-buffer contract comment.
- [x] #232 unused delegation exports removed.
- [x] #232 browser test mirrors now import production decisions; removed the orphaned prompt-classifier mirror.
- [x] #232 hydration lifecycle extracted from `public/app.ts` into `public/terminal-hydration.ts` with direct unit coverage.
- [x] #232 strict browser typechecking now gates migrated modules through `public/tsconfig.strict.json`.
- [ ] #232 lifecycle and resize ownership still remain in the 718-line terminal controller; extracting them is a separate high-risk browser refactor.
- [ ] full verification: the local Rust test build lacks the required verified Ghostty static bundle. `bun test` also has an existing control-API snapshot mismatch unrelated to this branch.
