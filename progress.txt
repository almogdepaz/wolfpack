# Progress Log

- 2026-02-25: Completed P0 UX-01 (mobile terminal WS stream). Replaced mobile fixed-interval /api/poll runtime path with WS streaming + reconnect states (live/reconnecting/offline/session-ended), added 2-minute retry budget with Retry now action, and added session-ended WS close handling/server alias coverage + integration tests.
- 2026-02-25: Implemented JWT auth middleware for protected HTTP `/api/*` routes and WebSocket upgrades (`/ws/*`) with HS256 validation, claim checks (`exp`/`nbf`/`iat` plus optional issuer/audience via env), and Authorization passthrough for Ralph peer aggregation; added integration coverage in `tests/integration/auth-middleware.test.ts` and verified existing WS/Ralph integration suites still pass.
- 2026-02-25: Expanded auth-middleware integration tests (11→31 tests): wrong-secret rejection, non-HS256/`none` algorithm rejection, malformed token structures (1/2/4 segments), empty bearer + non-Bearer scheme, `nbf`/`iat` future claims, array audience validation, 401 response shape (WWW-Authenticate header + JSON body), static asset auth bypass, WS expired/wrong-secret token rejection. Fixed ESM import hoisting bug (dynamic `import()` for env var setup).
- 2026-02-25: Cleaned up corrupted plan file — removed garbled checkbox entries (previous iteration's `<subtasks>` output was incorrectly appended as tasks). Marked UX-01 section header as done (strikethrough). Next real task: UX-02 (prompt-aware quick action bar).
- 2026-02-25: Completed P0 UX-02 (prompt-aware quick action bar). Added contextual `Yes/No/Enter/Ctrl+C` quick-action chips above the input bar on mobile terminal view. Chips appear when the last 5 lines of terminal output match interactive prompt patterns (y/n, yes/no, Press Enter, Continue?, approve, permission, waiting for input, etc.). Chips auto-hide when output no longer matches. `Yes` sends "yes"+Enter, `No` sends "no"+Enter, `Enter` sends Enter key, `Ctrl+C` sends C-c. Added 43 unit tests for the prompt classifier (`tests/unit/prompt-classifier.test.ts`) covering positive matches, line-window edge cases, negative matches, and real-world agent prompt patterns. Regenerated embedded assets.
- 2026-02-25: Completed UX-03 (per-session draft persistence). Input drafts persisted to localStorage keyed by `wp-draft|{machine}|{session}`. Draft saved on every keystroke, restored on session open/switch, cleared on successful send. Covers session switching, browser backgrounding, and page reload. No cross-session leakage — key includes both machine URL and session name.
- 2026-02-25: Completed UX-04 (terminal follow mode + scroll lock). Mobile terminal no longer yanks viewport when user scrolls up to read history. Follow mode tracks whether auto-scroll is active — disabled on user scroll-up, re-enabled when user scrolls back to bottom or taps "Live" pill button. Programmatic scrolls (from applyTerminalPane) are distinguished from user scrolls via a scrollTop setter patch. Follow mode resets to true on session open. Desktop xterm.js unaffected (handles its own scrollback). Regenerated embedded assets.
- 2026-02-25: Completed UX-05 (session triage signals in list). Added per-session triage classification: `needs-input` (prompt patterns), `error` (error patterns), `running` (tmux activity within 20s), `idle` (otherwise). Server-side classification in new `triage.ts` module, returned as `triage` field from `/api/sessions`. Sessions sorted by triage priority: needs-input > error > running > idle. Frontend renders colored badges (input=yellow, error=red, running=green, idle=gray) next to session names with matching dot colors and card border styling. Added 26 unit tests (`tests/unit/triage.test.ts`) and 7 integration tests covering classification and sorting. Regenerated embedded assets.
- 2026-02-25: Completed UX-06 (smarter reconnect model). Unified WS reconnect state machine across mobile and desktop: `live/reconnecting/offline/session-ended`. Desktop terminal now has exponential backoff with jitter (×1.8 factor, 0-200ms jitter), 2-minute retry budget (was infinite), and manual "Reconnect" button when budget exhausted. Shared constants (`RECONNECT_BUDGET_MS/BASE_DELAY_MS/MAX_DELAY_MS`), unified `setConnState()` renderer (replaced `setMobileConnState`), `retryConnection()` dispatcher routing to mode-specific retry. Visibility change handler resets desktop budget on foreground return. Banner copy improved: "connection lost" instead of "offline", "Reconnect" instead of "Retry now".
- 2026-02-25: Completed UX-07 (input safety for accidental sends). Configurable Enter behavior: `enterSends` setting (true=Enter sends/Shift+Enter newline, false=Enter newline/Shift+Enter sends). Defaults match current behavior (desktop=send, mobile=newline) but now user-toggleable in settings. Hold-to-send on send button: when `holdToSend` enabled and message >50 chars, requires 400ms press-and-hold with progress ring animation and haptic feedback. Both settings persisted to localStorage via `wp-effects`. Settings UI: new "Input" section in settings view with toggles. Placeholder text dynamically updates to hint at current Enter behavior. Regenerated embedded assets.
- 2026-02-25: Completed UX-08 (quick command palette). User-defined quick command chips in terminal view as horizontally-scrollable bar between action-bar and input-bar. Default set: status (git status), tests (bun test), build (bun run build), continue (/continue). Commands stored in localStorage (`wp-quick-cmds`). One-tap dispatches command text + Enter via `/api/send` with haptic feedback. Settings: "Quick Commands" section with add/edit/delete/reorder controls + reset to defaults button. Blue accent color (#66ccff) to distinguish from context-aware quick-actions (green/orange). Regenerated embedded assets.
- 2026-02-25: Completed UX-09 (session pinning & recents). Pin/unpin sessions via 📌 button on session cards — pinned sessions sort to top within each machine group. Recents queue tracks last-opened timestamp (recorded on openSession + switchSession), stored in localStorage (`wp-recents`, max 20). Session drawer now has Pinned/All tabs: Pinned tab shows pinned sessions + recent (non-pinned) sessions with time-ago labels; All tab shows every active session. Pin state stored in localStorage (`wp-pinned`). Regenerated embedded assets.
- 2026-02-25: Completed UX-10 (notification controls by severity). Split ATTENTION_PATTERNS into PROMPT_PATTERNS (y/n, approve, permission, waiting, Press Enter) and ERROR_PATTERNS (Error:, error[, failed, ❌) with separate toggles for each. Added per-session+signal-type cooldown/dedupe window (10-120s slider, default 30s) — prevents notification spam for the same session+type within the cooldown window. Added quiet hours by local device time (start/end time pickers, supports midnight wrapping). Consolidated notification haptic to use global `haptic()` function instead of raw `navigator.vibrate`. New "Notifications" settings section with toggles, range slider, and time pickers. All settings persisted to localStorage via `wp-effects`. Regenerated embedded assets.
- 2026-02-25: Marked P1 (Reliability and Flow Improvements) complete. All 5 subtasks (UX-06 through UX-10) were already implemented and committed. Updated roadmap section headers with strikethrough to reflect completion.
- 2026-02-25: Completed UX-11 (inline command snippets from terminal context). Lightweight heuristic classifier detects error patterns in last 20 lines of terminal output and suggests contextual next commands as dismissible chips. Covers npm errors (ERR/warn/ERESOLVE → npm ci/install), bun errors (→ bun install), git conflicts (CONFLICT/unmerged → git status/diff/abort), git push rejections (→ git pull), python import errors (→ pip install), permission denied (→ sudo/chmod), docker errors, rust/cargo errors, TypeScript errors, port-in-use, disk/memory issues, test failures. Orange accent chips (#ff8844) appear between quick-actions and command palette bars. Dismiss button (×) hides until suggestions change. Created `snippet-classifier.ts` module (pure function, importable for tests) + mirrored rules inline in frontend. Added 39 unit tests (`tests/unit/snippet-classifier.test.ts`) covering all error categories, negative matches, dedup, scan window, and max limit. Regenerated embedded assets.
- 2026-02-25: Completed UX-12 (better terminal rendering controls). Font size presets (small 12px/1.35, medium 13px/1.45, large 14px/1.55) as tappable preset buttons in settings. Line height paired with font size presets. Wrap behavior toggle (pre vs pre-wrap). Alternate monospace font stack option (JetBrains Mono/Fira Code/Source Code Pro/Cascadia Code). Reduced effects mode (suppresses all text-shadow glows and box-shadows). All settings apply to both mobile capture-pane rendering and desktop xterm.js PTY. Settings persisted to localStorage via wp-effects. resizePane() probe uses current preset values. xterm.js Terminal init reads saved presets. Mobile responsive override simplified to padding-only (font size now driven by preset classes). Regenerated embedded assets.
- 2026-02-25: Completed UX-13 (session timeline markers). Backend already had full infrastructure: `timeline.ts` (event recording with 2s dedup, max 100 events/session, triage transition detection), `triage.ts` (INPUT_PATTERNS/ERROR_PATTERNS classification), `serve.ts` (events recorded at session open, command send via HTTP and WS, triage transitions; `/api/sessions` enriches with last 5 events; `/api/timeline` endpoint for full history). Frontend had CSS for timeline dots (6 event types with distinct colors) and `timelineStripHtml()` rendering function. Completed 3 missing frontend integrations: (1) added timeline strip to single-machine session cards (`renderSingleMachineHtml`), (2) added drawer-timeline dots to session drawer items (`drawerItemHtml`), (3) added header timeline strip to terminal view header with 10s refresh polling via `/api/timeline`. Regenerated embedded assets.
- 2026-02-25: Completed UX-14 (recovery snapshots). Persists minimal per-session terminal tail snapshot to localStorage on each update (throttled to 2s). On session reopen, cached tail is shown immediately in the terminal while the live WebSocket stream reconnects. Works for both mobile (capture-pane text) and desktop (xterm.js buffer serialized to last 100 lines). Snapshots capped at 8KB per session. Stale snapshots cleaned on page load based on configurable TTL (default 5m, adjustable 1-30m via settings slider). Storage key format: `wp-snap|{machine}|{session}`. Flush-on-exit ensures snapshot is saved when navigating away from terminal view. Regenerated embedded assets.
- 2026-02-25: Completed UX-15 (mobile keyboard accessory row). Sticky accessory key row appears above mobile keyboard when input textarea is focused. Keys: Tab (green/special), Esc, Ctrl+C (red/danger), arrow keys (↑↓←→), and quick-insert characters (|, /, ~, - in orange). Special keys dispatch via `sendKey()` to tmux, insert keys insert at cursor position in textarea. Focus-preserving touch handling: `mousedown` preventDefault keeps keyboard open, `touchstart` fires action immediately for responsiveness with dedup guard against click double-fire. Hidden on desktop (media query + JS gate). Cleaned up on view switch and desktop terminal init. Horizontally scrollable with visual separators between key groups. Haptic feedback on key press. Regenerated embedded assets.
- 2026-02-25: Marked P2 (Advanced Solo Experience and Polish) complete. All 5 subtasks (UX-11 through UX-15) were already implemented and committed. Updated roadmap section headers with strikethrough to reflect completion. P0, P1, and P2 are now all done.
- 2026-02-25: Completed UX-16 (performance budget and telemetry). Client-side `wpMetrics` object tracks terminal render latency (p50/p95/avg from rolling 200-sample window), WS message count, reconnect count, send count, and send failure count per session. Instrumented `applyTerminalPane()` and desktop xterm.js `write()` for render timing, mobile+desktop WS `onopen` for reconnect detection, `sendMsg()`/`sendKey()` for send success/failure tracking. Metrics reset on session open. Hidden debug panel in settings (toggle "Performance metrics" checkbox) shows live-updating stats grid with 1s refresh interval. Timer auto-starts/stops with settings view visibility. Regenerated embedded assets.
- 2026-02-25: Expanded WS integration tests (27 tests total in ws-terminal.test.ts, up from 21). Added: mobile terminal session disappear/reconnect (client reconnects after session reappears, multiple terminals all get 4001 on session death), PTY grace period teardown (entry survives brief disconnect, entry torn down after 10s grace expires), multi-viewer PTY lifecycle (shared entry, partial disconnect doesn't trigger teardown, last viewer starts grace timer, reconnect cancels timer and reuses entry), rapid spawn failure edge cases (concurrent viewers all get 4001, spawn failure cleans up entry, pre-spawn disconnect handled gracefully). Full suite: 648 tests passing.
- 2026-02-25: Added 26 integration tests for prompt action dispatch and reconnect state transitions (`tests/integration/ws-dispatch.test.ts`). Covers: y/n key dispatch via WS terminal and HTTP /api/key (allowed + rejected keys), keyboard accessory input coordination (interleaved input+key+resize messages, prompt answer patterns, Ctrl-c cancel+retype, all WS_ALLOWED_KEYS), close code semantics driving client backoff decisions (4001 no-reconnect vs 1000 clean close vs spawn failure), terminal state machine transitions (unsized→sized, alive→dead, rapid dimension changes, post-close safety), PTY state transitions (entry creation/cleanup, grace period timer cancel on reconnect, rapid connect/disconnect cycle leak prevention, viewer count tracking), and rate limiting under dispatch (y/n burst capping, mixed input+key bucket sharing, token replenishment).
- 2026-02-25: Fixed 46 integration tests for prompt dispatch + reconnect state transitions (`tests/integration/prompt-reconnect.test.ts`). Added `__setTmuxSend` and `__setTmuxSendKey` test hooks to `serve.ts` (matching existing `__setTmuxList` pattern) so tests can mock real tmux send-keys calls. Tests cover: y/n text dispatch via `/api/send` with timeline recording, key dispatch via `/api/key` with allowlist enforcement, WS keyboard accessory input coordination (Tab/Escape/C-c/arrows/insert chars/mixed rapid messages), reconnect close code state transitions (4001 session disappear, reconnect after reappear, nonexistent session rejection), PTY spawn failure close codes (4001 consistency across consecutive failures), PTY grace period lifecycle (disconnect→grace→reconnect reuses entry, disconnect→grace→teardown after expiry), triage state machine transitions (classifySession priority rules, detectTriageTransition event recording), timeline deduplication/limits, and end-to-end prompt action→timeline event recording. 720 tests passing across full suite.
- 2026-02-25: Installed Playwright and created e2e test infrastructure. `playwright.config.ts` with 3 projects (iPhone SE 375×667, iPhone 14 390×844, desktop 1280×720). Test server harness (`tests/e2e/test-server.ts`) spawns real wolfpack server via bun with mock tmux stubs. Helper module (`tests/e2e/helpers.ts`) manages server subprocess lifecycle, viewport presets, and `waitForApp()` utility. Smoke test (`tests/e2e/smoke.spec.ts`) validates: page load, /api/info, /api/sessions with mock data, /api/send dispatch — 12 tests passing (4 tests × 3 viewports).
- 2026-02-25: Added e2e terminal connect flow tests (`tests/e2e/terminal.spec.ts`). 4 tests across 2 mobile viewports (8 total, desktop skipped — uses /ws/pty). Covers: session card click → terminal view navigation, WS output receipt (mock capturePaneAnsi returns content visible in #terminal), input send → output update (type command, click send, verify "command-output" appears), action bar visibility. Required: added `__setCapturePaneAnsi` and `__setTmuxResize` mock hooks to serve.ts, stateful pane content mock in test-server.ts (tmuxSend appends to pane), CORS fix for test mode (random port), fix duplicate `const PROMPT_PATTERNS` in index.html (renamed to NOTIF_PROMPT_PATTERNS). Regenerated embedded assets.
- 2026-02-25: Added e2e session switch tests (`tests/e2e/session-switch.spec.ts`). 5 tests across 2 mobile viewports (10 total, desktop skipped). Covers: open session drawer from terminal view (chip click → drawer opens), drawer lists all sessions (All tab shows both mock sessions), current session highlighted (.current class), switch session via drawer updates terminal content (test-project→another-project, verify pane content changes + chip label updates + drawer closes), switch session then send input targets new session (send command to switched session, verify original session unaffected by switching back).
